rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // API server handles all access via admin SDK (bypasses rules)
    // Block direct client access
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
